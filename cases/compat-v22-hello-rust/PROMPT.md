# compat-v22-hello-rust 执行目标

请作为 `claude` Agent 完成该真实测试任务。

## 目标

保留原 hello-rust 任务包结构，验证旧版 deliverables 与 execution 字段兼容。

## 边界

- 本用例不要求读取 ENV。
- 本用例不包含评审迭代要求。
- 不得修改 `spec.yaml`、`PROMPT.md`、`AGENTS.md`、`CLAUDE.md`。
- 交付结果必须通过 `spec.yaml` 中的验证命令。
