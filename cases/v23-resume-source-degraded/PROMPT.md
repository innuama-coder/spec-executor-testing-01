# v23-resume-source-degraded 执行目标

请作为 `claude` Agent 完成该真实测试任务。

## 目标

覆盖 resume 证据链连续、Agent source degraded 和 OpenClaw bounded failure。

## 边界

- 本用例不要求读取 ENV。
- 本用例不包含评审迭代要求。
- 不得修改 `spec.yaml`、`PROMPT.md`、`AGENTS.md`、`CLAUDE.md`。
- 交付结果必须通过 `spec.yaml` 中的验证命令。
