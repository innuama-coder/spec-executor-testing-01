# v23-review-multi-round 产品需求

## 目标

本用例验证 spec-executor 在 `2.3` 任务包下的核心行为：多条评审意见驱动多轮迭代，验证 item 状态、轮次、投送和验证证据。

## 交付要求

| 项目 | 要求 |
| --- | --- |
| 任务包版本 | `2.3` |
| executor | `codex` |
| 覆盖特性 | FR-006, FR-007, MET-002, MET-005 |

## 验收标准

任务执行完成后，交付物必须满足 `spec.yaml` 中定义的验证命令；涉及隐私或失败边界的用例必须产生可查询诊断，且默认输出不得包含 ENV 值。
