# v23-capture-failure-handoff 执行目标

请作为 `codex` Agent 完成该真实测试任务。

## 目标

覆盖 screen capture 产品化、默认安全投影、JOB 内失败 handoff 与 cleanup visibility。

## 边界

- 本用例不要求读取 ENV。
- 本用例不包含评审迭代要求。
- 不得修改 `spec.yaml`、`PROMPT.md`、`AGENTS.md`、`CLAUDE.md`。
- 交付结果必须通过 `spec.yaml` 中的验证命令。
